// Type inference in TypeScript allows the compiler
// to automatically determine a variable's type based on its value. 

let count = 42; // TypeScript infers (делает вывод) 'number' 

// Alternatively, you can explicitly(явный) declare the type:
let count1: number = 42; // Explicit type annotation

// Inference reduces redundancy, while explicit annotation provides clarity when needed.